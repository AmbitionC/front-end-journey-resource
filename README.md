# front-end-journey-resource

内容唯一真相源:学习路径 / 知识库 / 面试题库的 markdown 文章与图片。

- `interview/`、`knowledge/`:`{filePath}/{key}.md` 文章;每个模块根下 `_tree.json` 为目录树清单。
- `images/`:文章图片,发布时同步到 OSS。
- 写入路径:本地编辑 `git push`,或后台界面经 faas 提交。两者都收敛到本仓库,由 faas 同步到 OSS + DB。
